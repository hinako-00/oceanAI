# Ocean AI — 引き継ぎガイド（Claude / Codex 共通）

このファイルは、AIエージェント（Codex・Claude Code）がこのリポジトリで作業するための
唯一の引き継ぎ資料です。`CLAUDE.md` はこのファイルを指しているだけなので、
更新するときは必ずこちらを直してください。

最終更新: 2026-09-01 ／ 対象ブランチ `claude/blue-design-ui-fixes-ftwhw5`

---

## 1. このアプリの目的

個人のお客様（toC）向けの営業チームが、アポ前の準備・アポ後の振り返り・クライアント情報・
指導フィードバックを一か所に蓄積し、AIコーチへ相談できるようにするアプリです。

最終的に目指すのは「文章・PDF・案件メモを増やすほど、チーム固有の判断材料が育つ」状態です。
一般的な営業論を繰り返すのではなく、登録済みの自社資料・事例・フィードバックを優先して参照します。

## 2. 最初に読む場所

| ファイル | 何が書いてあるか |
| --- | --- |
| `README.md` | 画面、データ、認証、開発コマンド、画面の作りの全体像 |
| `lib/types.ts` | ドメインモデルと表示名。**保存済みデータと直結するキーは変更しない** |
| `lib/prompt.ts` | AIコーチの役割・出力形式・制約 |
| `lib/proposal.ts` | AIが作る保存候補（`save_proposal`）のスキーマと検証 |
| `lib/apply.ts` | 承認された項目だけを保存する唯一の入口 |
| `lib/context.ts` | 保存済みデータをAIへ渡す処理 |
| `lib/search.ts` | 保存済み知識の検索（表記ゆれの吸収を含む） |
| `lib/progress.ts` | 画面に出す「積み上げ」とカルテの埋まり具合を数える純粋関数 |
| `app/knowledge/page.tsx` | 資料・フィードバックを蓄積する画面 |
| `DEPLOY.md` / `NETLIFY.md` | 公開手順（自前サーバー・Docker／Netlify） |

## 3. 守るべき設計（変更しないこと）

- クライアント情報は `CustomerField` の `source`（`confirmed` 確認済み ／ `rep_report` 担当者報告 ／
  `ai_hypothesis` AI仮説 ／ `unconfirmed` 未確認）を必ず保つ。`value` と `source` は分離できない構造にしてある。
- **AIは保存候補を出せるが、自動で保存してはいけない。**
  `save_proposal` → 人の承認 → `lib/apply.ts` が唯一の書き込み経路。ここを迂回する実装を足さない。
- クライアントのセンシティブ情報から、営業能力や性格を断定しない。
- アポの結果と、営業行動の良し悪しを混同しない。根拠が足りなければ「未確認」として扱う。
- **数字も同じ。** 画面に出す件数は「行動の量」であって成果ではない。アポの結果と並べない
  （並べると記録を盛る動機になる）。画面にもその旨を明記してある。
- 秘密鍵、APIトークン、個人情報、クライアントデータをソースへ書かない・会話へ貼らない。

## 4. 知識の扱い（最重要）

知識は種別で保存する。**現状のコードで実装済みなのは次の4種別**（`lib/types.ts` の `KnowledgeType`）。

| 種別 | 使い方 |
| --- | --- |
| `product` | 商品資料・条件・仕様 |
| `rule` | 営業ルール・禁止事項 |
| `case` | 顧客事例・結果・再現条件 |
| `talk` | トーク例・質問例 |

構想にあって**まだ実装されていない**種別が2つある。足すときは `KnowledgeType` へ追加し、
`KNOWLEDGE_TYPE_LABEL`・`lib/prompt.ts`・`lib/context.ts` の参照ルールを併せて直すこと。

| 未実装の種別 | 意図 |
| --- | --- |
| `feedback` | 指導者からのフィードバック。案件・適用条件・根拠を残す |
| `framework` | ヒアリングや振り返りの型 |

`feedback` は特定案件の観察・仮説であり、普遍的な顧客心理や事実として一般化しないこと。
クライアント本人の発言、営業担当者の解釈、AIや指導者の仮説を必ず分けて扱うこと。

新しい知識機能を足す際も、文章をただ増やすより「どの案件で」「どんな条件で」「何を根拠に」
有効だったかを残せる設計を優先する。

## 5. 技術構成（実装の現状）

以前の引き継ぎ資料には Cloudflare Workers / D1 / AI Search / OpenAI Responses API と
書かれていたが、**現在のコードはそうなっていない**。実際の構成は次のとおり。

- Next.js 15 App Router / React 19 / TypeScript
- **Claude API（`@anthropic-ai/sdk`）**。`ANTHROPIC_API_KEY` を `.env.local` か
  ホスティング側の環境変数で設定する。`lib/anthropic.ts` にモデルとトークン上限。
  キーが未設定でも画面は開き、AIの応答だけが使えない。
- 永続化は**JSONファイル**（`data/*.json`、`lib/store-file.ts`）が既定。
  サーバーレス（Netlify Functions）では **Netlify Blobs**（`lib/store-blobs.ts`）へ切り替わる。
  切り替えの判定は `lib/storage-driver.ts`（環境変数だけを見る純粋関数、テスト済み）。
  Postgres への移行の下地が `lib/store-postgres.ts.example` にある。
- 公開手段は自前サーバー / Docker + Caddy（`DEPLOY.md`）と Netlify（`NETLIFY.md`）。
  `wrangler.jsonc` は存在しない。Cloudflare 向けの記述を見つけたら、それは古い情報。

本番公開、データの破壊的変更、ホスティング設定・シークレットの変更は、
必ずユーザーの明確な許可を得てから行う。

## 6. 開発と検証

```bash
npm install
npm run typecheck   # tsc --noEmit
npm test            # node:test（純粋ロジックのみ・現在60件）
npm run build       # Next.js の本番ビルド
npm run seed        # 任意：デモデータ投入（既存データは上書きしない）
npm run dev         # http://localhost:3000
```

`npm run lint` は ESLint が未設定で対話プロンプトに落ちるため、現状は使えない（既知）。

初回アクセスは `/setup` に飛び、最初の管理者アカウントを作る。
以降は「メンバー管理」から追加する。

---

## 7. このセッションでやったこと（2026-09-01）

ブランチ `claude/blue-design-ui-fixes-ftwhw5`、コミット3件。型チェック・テスト60件・ビルドはすべて通過。
実機幅390pxと1440pxで Playwright を使って画面を確認済み。

### 7-1. 配色をブルーデザインへ入れ替え（3案 → ディープネイビーに決定）

ティール（`#0d7d8c`）系から青系へ全面的に入れ替えた。性格の違う3案を用意し、
利用者が「自分の設定 → 画面の配色」で切り替えられる。**既定はディープネイビー**。

| キー | 名称 | 性格 |
| --- | --- | --- |
| `navy`（既定） | ディープネイビー | 濃紺と強い罫線。一覧・表を数多く読む人向け。角丸10px |
| `ocean` | オーシャンブルー | 白地に澄んだ青。文字と背景の差が一番大きい。角丸14px |
| `sky` | ソフトスカイ | 水色寄り・線が弱め。長時間見る人向け。角丸18px |

- 色の実体は `app/globals.css` 冒頭。`:root`（＝既定の navy）と `[data-theme='ocean' | 'sky']`。
  3案とも light / dark の対を持ち、端末のダークモードに追従する。
  ダーク側の `:root:not([data-theme='ocean']):not([data-theme='sky'])` は
  「既定、または未指定」だけに当てるための除外なので、案を増やすときはここも直す。
- 案の一覧・名前・説明・見本の色は `lib/theme.ts`。画面の文言はここだけ直せば済む。
- 選択値は端末の `localStorage`（キー `ocean-ai-theme`）。サーバーには送らないので
  チームの他の人の画面は変わらない。
- 初回描画のちらつきを防ぐため、`app/layout.tsx` が `<body>` の先頭で `<html data-theme>` を
  先に書き込む。**キー名を変えるときは `lib/theme.ts` と `app/layout.tsx` の両方を直すこと。**

**画面で色を直接書かないこと。** 必ずトークン（`var(--accent)` など）を使う。
直接書くと3案のどれかで読めなくなる。押した状態は `--press` / `--accent-press`。

### 7-2. 操作の不具合を修正

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 押しても色が変わらない | 変化が `opacity: 0.75` だけだった | 種類ごとに押下時の面の色（`--press` / `--accent-press`）を当てた |
| タップ後に色が戻らない | `:hover` が全端末に効き、指で触ると張り付いていた | `@media (hover: hover) and (pointer: fine)` の中だけに閉じた |
| 閉じるボタンが押せない | 40px・枠なし・背景が周囲とほぼ同色で、`flex: none` がなく見出しが長いと潰れていた | 44px四方＋枠付き＋`flex: none`。実機幅390pxで当たり判定と動作を確認 |
| スクロールすると閉じるボタンが画面外へ | シートの見出しが一緒に流れていた | `.sheet-head` を上端に固定。長い見出しは省略表示 |
| 選択中がどれか分かりにくい | 色だけの違いだった | 線・太字・チェックなど色以外の手がかりを追加 |
| PCのボタンが小さい | 36px / 30px | 38px / 32px、ボトムタブ 60→62px |
| Tab移動で現在地が見えない | `:focus-visible` がなかった | 全体に追加 |

**新しくCSSを書くときの約束**（`README.md` の「画面の作り」にも記載）:

- 押せるものはモバイルで最低44px四方（`var(--tap)`）。入力欄の文字は16px以上。
- `:hover` は必ず `@media (hover: hover)` の中に書く。
- 装飾の動きは `@media (prefers-reduced-motion: reduce)` で止められるようにする。
  止めても位置と色は変えないので意味は落ちない。

### 7-3. 「積み上げが見える」画面

- **相談画面の頭**（`app/components/WeeklyProgress.tsx`）に直近7日の積み上げ。
  記録したアポ / 増やした知識 / 担当クライアント数と、曜日別の記録数。
  会話が始まったら引っ込む。
- **クライアントのカルテ**（`app/components/Coverage.tsx`）に「確からしさ」の積み上げ棒。
  一覧にも小さい版（`CoverageMini`）を置いた。
- **自社営業知識**に種別ごとの登録数。どの種類が手薄かが分かる。

**ここで直した数え方のバグ**: 以前は「値が入っている項目数」を確認済みとして数えていたため、
AIの仮説だけで埋まったカルテが「確認済み 8/13」に見えていた。いまは情報源ごとに分けて数え、
値があっても `source` が `unconfirmed` のものは未確認として扱う（`lib/progress.ts`）。

数える処理は `lib/progress.ts` の純粋関数に寄せ、`tests/progress.test.ts` で検証している。
**日付の区切りは端末のローカル時刻**で行う。サーバー（UTC）で区切ると、
日本時間の夜に入れた記録が前日に寄る。`/api/bootstrap` は集計せず作成時刻の配列
（直近30日ぶん）を返し、クライアントで集計する。

### 7-4. 手ざわり

- 保存が通ったときにチェックを描く合図（`app/components/SaveFlash.tsx`）＋
  対応端末での短い振動（`lib/haptics.ts`）。知識の登録・更新、プロフィール保存、
  パスワード変更に付けた。**iOS Safari は Vibration API 非対応なので振動は鳴らない**（想定内）。
- 保存候補を承認したとき、パネルが消える前に「何件を保存したか」を一拍見せる。
- いちばん新しい発言だけ下から立ち上がる（過去の相談を開いたとき全部が動くと目が滑るため）。
- ボトムタブのアイコンが押した瞬間に沈む。
- 上記の動きはすべて `prefers-reduced-motion` で止まる（実測で確認済み）。

### 7-5. グラフの色の決め方

意味に合わせて役目を分けている。**目視で色を決めないこと。**

| 用途 | 色の役目 | トークン |
| --- | --- | --- |
| カルテの確からしさ（確認済み ＞ 担当者報告 ＞ AI仮説） | ordinal（順序のある1色の濃淡） | `--ord-1` 〜 `--ord-3` |
| 未確認・未入力 | 色を持たせず下地として置く | `--bg-subtle` |
| 1系列の棒（曜日別の記録数、種別ごとの件数） | 1色 | `--accent` |

`--ord-*` は3案ぶん、light / dark それぞれで「単一色相・明度が単調・薄い端が下地から2:1以上」を
満たすことを確認した値。増やすときも同じ条件で確かめること。
**色だけで意味を運ばない。** どのグラフにも数値の文字表示を必ず添える。

---

## 8. 次にやること

1. **AIの知識向上**。ユーザーから資料が渡される予定。`feedback` / `framework` の種別追加、
   知識の検索精度・引用表示・重複検知の強化がここに紐づく。
2. `ANTHROPIC_API_KEY` を設定したうえで、実際のAIコーチ応答と知識参照の統合テスト。
   保存候補（`save_proposal`）→ 承認 → `lib/apply.ts` の一連が本番のモデル応答で
   通ることは、まだ実機で確認できていない。
3. 「積み上げ」の続き（未着手）:
   - アポを保存したとき、そのアポで埋まったカルテ項目を光らせる。
   - 引っぱって更新は**見送った**。iOS Safari の標準のプルダウン更新と競合して
     どちらも中途半端に効くため。入れるなら一覧の頭に更新ボタンを置く形が安全。
4. 見送った案（記録として）: ポイント・バッジ・メンバーのランキング。
   「アポ結果と営業行動の良し悪しを混同しない」に反し、記録を盛る動機になるため入れない。

## 9. 既知の穴

- `npm run lint` が使えない（ESLint 未設定。`next lint` は Next.js 16 で削除予定）。
- `lib/types.ts` の `KnowledgeType` が4種別しかない（第4節）。
- 保存候補の承認後の表示（`ProposalPanel` の完了状態）は、APIキーがない環境では
  実物で確認できていない。コードの経路のみ確認済み。
- `data/*.json` は `.gitignore` 済み。ローカルのデモデータはコミットされない。
